import React from 'react'
import {useSelector, useDispatch} from 'react-redux';
import EntryPage from '../../pages/Anonymous/EntryPage';
interface EntryPageContainerProps {
    
}

const EntryPageContainer: React.FC<EntryPageContainerProps> = ({  }) => {
    return (
        <EntryPage/>
    );
}

export default EntryPageContainer;